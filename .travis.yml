language: python
python:
  - "3.6"

services:
  - docker

before_script:
  - docker-compose up -d

# command to run tests
script:
  - docker ps
  - ip addr
  - python elasticache_auto_discovery/__init__.py "localhost:11212"

after_script:
  - docker-compose down
